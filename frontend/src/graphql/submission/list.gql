query SubmissionListGQL($page: Int!,$date: String!) {
	submissionList(page: $page,date: $date) {
		maxpage
		submissionList {
            submissionId
            submitTime
            user
			problem
            judgeStatus
            language
			color
		}
	}
}
