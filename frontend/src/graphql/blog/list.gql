query BLogListGQL($page: Int!) {
	blogList(page: $page) {
		maxpage
		blogList {
            id
            title
            user{
                username
            }
            vote
            view
		}
	}
}
